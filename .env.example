# Copy to .env and fill in. Never commit real .env (gitignored).
# Loaded automatically by nex CLI / GrokEscalator (does not override existing shell exports).

# --- Active now: direct xAI Grok escalation ---
XAI_API_KEY=
# Optional model id on api.x.ai (default: grok-3)
# NEX_GROK_MODEL=grok-3

# --- Tabled for later (ignored by code today) ---
# OPENROUTER_API_KEY=
# OPENAI_API_KEY=   # alternate name only if you point it at xAI; base URL is still api.x.ai

# Optional agent flags
# GROK_IN_LOOP=true
# NEX_MAX_GROK=3
