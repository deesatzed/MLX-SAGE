# Handoff Packet — MLX-SAGE

## Snapshot

| Field | Value |
|-------|-------|
| Date | 2026-08-07 |
| Branch | `main` @ `bec3896` (tracks `origin/main`) |
| Env | local dev |
| Status | 🟡 Yellow |
| Tests | **22/22 PASS** (`pytest tests/`) |
| Working tree | Clean for product code; **untracked** onboard artifacts from this session (see below) |

**Why not 🟢:** Critical honesty debt remains (TUI synth approvals); no CI; no LICENSE; product identity split. Tests green but release/ops incomplete.

---

## What I Was Doing

- **Goal:** `/build` Stage 2 **Onboard to this repo** (role=SWE, checkpoints=defaults).
- **Progress:** Full chain executed:
  1. `docs-context --mode=project-load`
  2. `discovery --mode=deepdive` → `Analysis_2026-08-07.md`
  3. `discovery --mode=forensics` → `Forensics_2026-08-07.md`
  4. `governance --mode=debt` → `Debt_2026-08-07.md` + `.governance/debt.json`
  5. `governance --mode=assumptions` → `Assumptions_2026-08-07.md` + `.governance/assumptions.json`
  6. `docs-context --mode=handoff` → this file
- **Last verified working state:** imports OK; pytest 22/22; policy BLOCK on `.env` via real observer; no live MLX regen or XAI call this session.

---

## What's In Flight

| State | Items |
|-------|-------|
| Staged | nothing |
| Unstaged product code | nothing (main clean at bec3896) |
| **Untracked (this onboard)** | `Analysis_2026-08-07.md`, `Forensics_2026-08-07.md`, `Debt_2026-08-07.md`, `Assumptions_2026-08-07.md`, `Handoff_2026-08-07.md`, `.governance/` |
| WIP notes | User declined intermediate checkpoint on handoff step; chain completed by judgment to close onboard goal |

**Do not treat onboard markdown as merged product truth until user commits them.**

---

## Blockers

| Blocker | Owner / unblock |
|---------|-----------------|
| No hard external blocker for continued local build | — |
| Live Grok V8 needs `XAI_API_KEY` | user provides key → run escalate once → update PROGRESS V8 |
| Live Sage TUI needs complete local MLX weights | `nex sage models` READY path |

---

## Risks & Unknowns

| Risk | Evidence | Mitigation |
|------|----------|------------|
| TUI “Sentinel queue” is synth | `nex/tui.py` `fake_dec` | D-001/D-002 — remove or fully wire |
| Identity split Sage vs Nex packaging | README vs pyproject | D-003 |
| No CI / LICENSE | repo root | D-006/D-007 |
| PII in sessions/ | sage chat | A-0011 — confirm gitignore (sessions/** present) |
| MLX gen not re-run this session | deepdive unknown | smoke `sage tui` if model READY |

---

## Key Files Reference

| Role | Path |
|------|------|
| Entry | `nex/cli.py`, `nex/__main__.py` |
| Sage | `nex/sage/{tui,dialog,partner,local_models}.py` |
| Hive / mission | `nex/hive/cell.py`, `nex/mission.py` |
| Superintendant | `nex/superintend.py`, `nex/propellant.py`, `nex/wattos.py`, `nex/sentinel/*` |
| Engine / agent | `nex/engine.py`, `nex/agent.py`, `nex/tools.py` |
| Config | `pyproject.toml`, `nex/config.py` |
| Gov product | `PROGRESS.md`, `DECISIONS.md`, `EXPANSION_PLAN.md` |
| Onboard artifacts | `Analysis_*`, `Forensics_*`, `Debt_*`, `Assumptions_*`, `.governance/` |
| Tests | `tests/` |

---

## Next Actions (Prioritized)

### P0
1. Decide: remove TUI `fake_dec` path **or** wire real `policy.evaluate` + gate tools (debt D-001/D-002).
2. Add `LICENSE` (MIT) matching README claim (D-007).
3. Align `pyproject.toml` description + CLI help with MLX-SAGE dual-surface story (D-003).

### P1
4. Minimal CI: run `pytest tests/` on push/PR (D-006).
5. Smoke tests for `server` `/health` and MCP import (D-005).
6. Commit or discard onboard artifacts (user choice).

### P2
7. Refresh `ARCHITECTURE_MERGE.md` gaps; real screenshots or demote media claims.
8. WattOS unify on native agent; Grok V8 when key present.

**Suggested next stage:** Stage 3 (enhance/repair) focused on backend honesty + UX truth for TUI approvals, **or** Stage 2 “Decide next slice” → `planning --mode=plan` for P0 only.

---

## Open Questions / Decisions Needed

1. Commit onboard reports into repo or keep local-only?
2. Is Nex multi-model still co-equal product, or Sage-primary with Nex as substrate?
3. Authority to delete synth TUI queue vs finish full Sentinel HUD?
4. Is CI required before more features?

---

## Verification Commands

```bash
cd /Volumes/WS4TB/waswiki/repos2mine/MLX-SAGE
export PYTHONPATH=.

# Health
python -m pytest tests/ -q
python -m nex.cli --help
python -m nex.cli sage models

# Policy honesty smoke
python -c "
from pathlib import Path
import tempfile
from nex.sentinel.enforcer import FileEffectObserver
from nex.sentinel.policy import SentinelPolicy, PolicyAction
ws = tempfile.mkdtemp()
o = FileEffectObserver(ws); o.snapshot()
Path(ws,'.env').write_text('x')
d = SentinelPolicy().evaluate(o.diff())
assert d.action == PolicyAction.BLOCK
print('policy BLOCK OK', d.reason)
"

# Forensics residual
rg -n 'fake_dec|fake_fx' nex/tui.py
```

---

```json
{
  "mode": "handoff",
  "branch": "main",
  "commit": "bec3896",
  "status": "yellow",
  "blockers": 0,
  "tests": "22/22",
  "next_actions": [
    "D-001/D-002 TUI synth approvals",
    "D-007 LICENSE",
    "D-003 product identity align",
    "D-006 CI"
  ],
  "unknowns": 2,
  "artifacts": [
    "Analysis_2026-08-07.md",
    "Forensics_2026-08-07.md",
    "Debt_2026-08-07.md",
    "Assumptions_2026-08-07.md",
    "Handoff_2026-08-07.md",
    ".governance/debt.json",
    ".governance/assumptions.json"
  ]
}
```
